{
  "name": "bloglist-expansions",
  "version": "1.0.0",
  "description": "Blog list with delete and update functionality",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "cross-env NODE_ENV=test jest --verbose --runInBand",
    "test:watch": "cross-env NODE_ENV=test jest --verbose --runInBand --watch"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4",
    "cross-env": "^7.0.3",
    "mongodb-memory-server": "^8.15.0"
  }
}